# This project uses conda-forge for dependency installation.
# Create/update the environment with:
#
#   conda env create -f environment.yml
#   conda env update -f environment.yml --prune
#
# Avoid installing this file with pip; use environment.yml instead.
